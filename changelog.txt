important - If you are deploying functions, you must update the firebase-functions SDK to the latest by running "npm i --save firebase-functions@latest" inside your functions directory. This enables faster functions deployment.
feature - Make the environment variables $RESOURCE_DIR, $GCLOUD_PROJECT, and $PROJECT_DIR available inside the "build" script of functions/package.json
feature - Add "--host" flag to `firebase experimental:functions:shell` and `firebase serve --only functions` to allow specification of server host
feature - Add eslint to the starter template for the functions folder to catch potential bugs in functions code prior to deployment.
feature - Add support for hosting and functions source folders to be symbolic links.
fixed - Fixed bug where symbolic links in functions source folder produced errors.
