feature - Added `--interactive` flag to force interactive mode for faulty TTY detection.
fixed - Rewrites, redirects, and headers now deploy properly if `"public"` key is absent from `firebase.json` but `--public` flag is provided.
fixed - Malformed `firebase.json` now errors out instead of hanging.
