specVersion: "v1alpha"
serviceId: "__serviceId__"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "__databaseId__"
      cloudSql:
        instanceId: "dataconnect-test"
connectorDirs: ["./connector"]
