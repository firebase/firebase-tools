specVersion: v1alpha
serviceId: us-east
connectorDirs:
  - ./connectors/a
schema:
  main:
    source: ./schema
    connection:
      connectionString: postgresql://postgres:mypassword@localhost:5432/emulator?sslmode=disable
