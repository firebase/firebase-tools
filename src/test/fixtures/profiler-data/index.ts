import { resolve } from "path";

/**
 * A sample JSON file for profiler input.
 */
export const SAMPLE_INPUT_PATH = resolve(__dirname, "sample.json");

/**
 * A sample JSON output file generated by the profiler.
 */
export const SAMPLE_OUTPUT_PATH = resolve(__dirname, "sample-output.json");
