/**
 * 1. Copy template, and then fill in the initialization code using the object provided by Firebase CLI
 */
// const { getSdkConfig, AppPlatform } = require('../../src//management/apps');
// const { doSetup } = require('../../src/init/features/project');
// const { getOrPromptProject } = require('../../src/management/projects');
const { needProjectId } = require('../../lib/projectUtils');

needProjectId({ });

// doSetup({}, {}, {});

// const projectId = getOrPromptProject();
// const sdkConfig = getSdkConfig({}, AppPlatform.WEB);
