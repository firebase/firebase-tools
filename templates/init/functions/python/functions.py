# # from firebase_functions import functions

# Welcome to Cloud Functions for Firebase for Golang!
# To get started, simply uncomment the below code or create your own.
# Deploy with `firebase deploy`

# # @functions.https(memory_mb=256)
# def http_function(request):
#   return 'Hello world'

# TODO hard code a new http server (via Flask?) that exposes the following routes for now
# http://localhost:${adminPort}/__/quitquitquit
#   => Proces quit
# http://localhost:${adminPort}/__/functions.yaml
#   => Return the functions.yaml file currently hard coded
# http://localhost:${adminPort}/<projectId=*>/<region=*>/<functionName>
#   => Invoke the function, in this case just return 'Hello world' for now

